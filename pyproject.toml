[build-system]
requires = [
  "setuptools>=61",
  "wheel",
  "cmake>=3.15",
  "ninja",             # optional, speeds up parallel builds
  "pybind11>=2.6.0"    # for the binding headers
]
build-backend = "setuptools.build_meta"